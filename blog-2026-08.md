# Subcast 八月更新：模型栈换代、AI 字幕润色，长视频更快出字幕，AI 助手可本机接入

Subcast 是一款本地优先的桌面端音视频转写 / 翻译 / AI 总结工具（基于 Nuxt 4 与 Electron 构建），所有推理都在你自己的电脑上完成，不上传云端。八月（8/16–8/20）我们连续发布了 **v0.5.0、v0.5.1、v0.5.2、v0.5.3**，核心是一次「模型栈换代」+「模型调用链路重构」+「本机 AI 助手接入」。下面按主题讲清楚这个月到底更新了什么、优化了什么。

> 数据范围：2026-08 的发布变更，横跨四个版本。本期仅发布 macOS 版（Windows 构建暂缓）。

---

## 一、模型栈升级：从 2024 代际跳到 2026 代际

### 1. 大模型：Qwen2.5 → Qwen3
- 档位从 3B/7B/14B 调整为更接近真实尺寸的 **4B/8B/14B**（Q4_K_M 量化）。
- 显式关闭思考模式并做防御性剥离，翻译 / 摘要解析零改动。
- 老用户设置自动迁移（`3b→4b`、`7b→8b`），无需手动重选。
- llama.cpp 锁定到 **b10435**，上下文设为 8192 以兼顾 8GB 机型（16k 预分配会挤爆低配机器）。

### 2. 语音识别：内置默认引擎从 Whisper 换成 SenseVoice-Small
- **开箱即更友好**：默认内置模型从 Whisper base 换成 SenseVoice-Small，**中文用户首启就走更快更准的中文引擎，不用自己挑模型**。
- **中文更准、推理更快**：SenseVoice 中文准确率优于 Whisper-large，**CPU 推理比 Whisper 快约 15 倍**。
- `auto` 模式按音频语言自动分发 SenseVoice / Whisper；整任务语言锁定，修掉了中英混合转写「串语言」的问题。
- 内置模型置换（净 +90MB）并接入首启向导下载。

---

## 二、AI 字幕润色层

转写完成后用大模型做一层校对：**纠正同音错字、把被拆散的英文拼回单词、规范标点**。关键是字幕条目与时间戳严格 **1:1 对齐**，支持双语导出和播放器内变体切换，还能配置领域术语提示词。底层新增 `polish_tasks` 表与五语言 UI。

### 润色前后对比

下面是一段真实风格的原始转写（SenseVoice 输出）与润色后的对照。三类典型错误都覆盖了：

| # | 原始转写 | 润色后 | 修的问题 |
|---|------|------|------|
| 1 | 我们今天用的模型是**球叉运算**里的 **Q wen three** | 我们今天用的模型是**布尔运算**里的 **Qwen3**。 | 同音错字 + 英文拼回 |
| 2 | 这个**三 d** 打印的零件误差很小 | 这个 **3D** 打印的零件误差很小。 | 英文被拆散 |
| 3 | 我们打开 **f f m peg** 把视频转成 **m p four** | 我们打开 **ffmpeg** 把视频转成 **mp4**。 | 英文被拆散 |
| 4 | 他说**音化同步**很重要但没说为什么 | 他说**音频同步**很重要，但没说为什么。 | 同音错字 + 标点 |
| 5 | **庐州府学**是宋代的书院里面还讲 a i | **庐州府学**是宋代的书院，里面还讲 AI。 | 专名 + 英文（见下方提示词） |

**为什么第 5 行能认对「庐州府学」？** 润色提示词里可以填领域术语（本例填了 `庐州府学、布尔运算`）。代码注释里明确写着：用户提供的专名提示是「专有名词纠错最大的质量杠杆」——模型优先按这些写法修正，拿不准的本意宁可保留原文也不瞎改。

**「1:1 对齐」意味着什么？** 上面 5 行润色后仍是 5 行，每行的时间戳（起始与结束毫秒）一字未动。所以润色层可以和原始字幕逐行对照，做双语导出、在播放器里一键切换「原文 / 润色」变体，绝不会因为多一句少一句而对不上轴。

---

## 三、模型调用链路提速（实测）

这次最大的「体感优化」来自把推理服务**常驻 + 内存切片**，而不是每段音频重新拉起进程：

| 指标 | 旧（命令式拉起） | 新（常驻服务） | 提升 |
|------|------|------|------|
| Whisper 每段 | 2.75–4.9s | **2.0s** | 25–60% |
| SenseVoice 每段 | — | **0.24s / 10s** | — |
| 片段间循环开销 | — | p50 = **7ms** | 流水线无空等 |
| 润色批次（25 条/批） | 重试放大最坏 25× | **8–10s，零错配** | 稳定 |

其他提速点：
- 大模型改用 **JSON Schema 约束解码**，消灭数量不匹配导致的重试放大（最坏一批评 25 倍重试）。
- llama-server 参数调优 + **双伴随进程尾段预热**，转写→润色零冷启动（冷启动从 ~20s 压到 0.76–1.5s 热启动）。
- 完整同机实测（M3 Pro，59s 音频）：转写 2.1s、翻译 24.3s、润色 16.9s、洞察 23.5s。

---

## 四、模型调用控制面：让一切可复现、可缓存、可审计

v0.5.1 把「调哪个模型、用什么提示词、出什么产物」抽象成一张统一契约（模型调用控制面）：

- **调用规格 + 指纹**：每次大模型任务在入队时就锁定模型、提示词版本、schema 版本、目标语言、生成参数。模型 / 语言 / 设置一变，就不会复用旧产物。
- **模型租约语义**：伴随进程从「有个服务在跑」升级为「跑的是这次请求要的模型」，切换模型自动停旧起新，崩溃恢复不串模型。
- **硬件感知运行时档案**：苹果自研芯片走 Metal（`--n-gpu-layers 999`、双槽、每槽 8192 上下文），入门机型自动降并发 / 降上下文避免内存溢出；状态接口如实上报「请求的后端」vs「验证到的后端」。
- **Token 感知的洞察生成**：长转录不再用单一上下文硬扛，而是按 Token 预算（每窗 8192 上下文）分窗后再汇总，长任务只推阶段 / 进度，不把中间文本当最终结果。
- **隐私护栏**：日志与诊断包默认不写入转写文本、提示词、模型输出、文件路径（调试模式关闭时严格脱敏）。

---

## 五、v0.5.2：长视频更快出字幕 + 状态更透明

### 1. 批处理「先出字幕后增强」策略
长音视频 / 批量任务现在**先让整批文件尽快出「原文 + 基础字幕」，再继续跑**翻译、润色、洞察、说话人识别等增强任务。用户可以更早打开单个视频看字幕，不用等整批跑完。新增 `long_media_fast_first` 预设，多文件场景默认启用。

> 实测：10 个文件批量，日志显示 **10 次转写全部完成后才开始 10 次翻译**，零失败。

### 2. 模型运行状态可视化
设置页拆成「**转写模型运行状态**」和「**AI 模型运行状态**」，分别显示 Whisper/SenseVoice 与 Qwen3 是否在跑、已加载还是等待自动释放；并说明翻译 / 润色 / 摘要分别用哪个 Qwen3 档位，提示缺失模型或回退策略。

### 3. 状态更清楚、更少误判
- 播放器和任务队列的模型错误、长洞察进度、翻译状态文案更明确，减少「是不是卡住了」的误判。
- 模型资源状态在开发热更新或运行时持有者丢失引用时，仍从 Subcast 自己的伴随进程兜底判断，避免运行中误报「未加载」。

---

## 六、官网与文档站

- 文档站（**VitePress 中英双语**）上线，README 重写。
- 官网品牌化（对比组件、logo、主题）。
- 接入 **51la** 与**百度统计**，便于了解访问情况。

---

## 七、v0.5.3：给 AI 助手一个安全的本机入口

v0.5.3 解决的是另一个实际问题：很多人已经在用 Codex / Claude Desktop 处理本地材料，但音视频理解这一步如果走云端，就和 Subcast 的隐私前提冲突。现在 Subcast 可以作为本机媒体理解引擎，被 agent 调用，但不暴露桌面内部 token。

### 1. 设置页新增「AI 助手」

用户需要在「设置 → AI 助手」里显式启用本机访问。授权只在当前应用运行期间有效，Subcast 退出后自动失效。这个设计故意让访问有边界：agent 可以处理你明确交给它的本地媒体，但不能绕过用户授权长期驻留。

### 2. MCP Server 随安装包提供

安装版会带上 `subcast-mcp` 启动器。Codex、Claude Desktop 等支持 MCP 的客户端可以通过它调用 Subcast 工具：导入媒体、查看状态、启动转写 / AI Insights、等待完成、导出媒体包。

源码开发也支持：

```bash
pnpm build:desktop:main
pnpm subcast:mcp
```

### 3. Codex skill 和媒体包

仓库内置 `skills/subcast`，可以通过：

```bash
pnpm skill:install
```

安装到 `~/.codex/skills/subcast`。之后可以直接让 Codex 做本机转写、导出字幕、生成媒体包或基于证据做总结。

媒体包不是「又一个 zip」。它把音视频拆成可追溯材料：`transcript.md`、`subtitles.srt`、`sources.json`、`manifest.json`、`summary.md`、`chapters.md` 和 `deliverable.md`。后续 agent 回答问题时可以落回时间戳，而不是凭感觉编一段。

### 4. Harness：把 agent 工作流变成可验收对象

新增 harness 命令覆盖预检、app-backed 端到端流程、bundle 导出和评分：

```bash
pnpm harness:preflight-run
pnpm harness:app-flow
pnpm harness:export-bundle
pnpm harness:score-bundle
```

这让「agent 能不能真的处理一段本地视频」不再是演示，而是可以复跑、可以打分、可以在发布前卡门禁的东西。

---

## 工程质量

- 全量单测、类型检查、代码规范保持绿色（0.5.3 检查点：**660 passed / 2 skipped**，lint 0 errors，docs build 通过）。
- macOS 打包按发布流程验证：whisper-cli / llama-server rpath、`--help`、无构建机路径泄漏。
- 生产批量导入复验**零降级、零崩溃**。

---

## 小结

八月对 Subcast 来说是一次「内功」大更新：模型栈从 2024 代际跳到 2026（**Qwen3 + SenseVoice**），AI 字幕润色让输出更干净，常驻推理 + 控制面重构让转写 / 翻译 / 摘要又快又稳又可复现。下一步会聚焦 **Windows 构建恢复**与**说话人识别在长视频上的体验**。

*数据来源：仓库 2026-08 git 提交、CHANGELOG、性能基线文档（docs/performance-baseline.md）与对应设计文档。*
